# Work Order — wo-04-01

**Order ID:** wo-04-01  
**Section:** section-04-ux-recovery  
**Iteration:** `.grok/org/improve-grok-build-interaction/iterations/section-04-ux-recovery/iteration-01.md`  
**Status:** done  
**Worker role:** implementer

## Task

Add (or complete) **pure, unit-testable** UX recovery helpers for **Grok Build** send/auth failures so the tool window can stop using bare `Error: ${e.message}` for session/proxy classes.

### Requirements

1. **Discover first** whether section-01 (or current tree) already added a pure auth recovery formatter (search for helpers near `GrokBuildAuth`, settings, or `ui/*Ux*`).  
   - If present: **reuse** it — add only thin wrappers or missing cases; **do not fork** a second string catalog.  
   - If absent: create a small pure object (e.g. `GrokBuildSendUx` under `src/main/kotlin/com/waryway/gab/ui/`) with recovery-oriented copy for:
     - missing session / no usable session → coach `grok login` + auth path
     - expired / near-expired session (if distinguishable from auth APIs)
     - HTTP 401/403-class failures from cli-chat-proxy → re-login recovery
     - generic network / unreachable-style failures (distinct from Local LLM offline copy)
2. Keep **Local LLM** path labels and offline/blank-URL helpers in `LocalLlmSendUx` as the source of truth. Only extend Local helpers if a proven gap remains (prefer no change if tests already cover labels).
3. Prefer **pure functions** (no Swing/IDE). Mirror the `LocalLlmSendUx` style.
4. **Unit tests** for every new/changed pure helper (`LocalLlmSendUxTest` and/or new `GrokBuildSendUxTest` / similar).

### Coordination

- Section-01 owns auth.json parser + session usable checks. You may **read** those APIs; do not rewrite parser internals.
- Section-04 owns UX string surfaces and send-path formatting. Single source of truth for recovery strings.

## Scope

**Edit only:**

- `src/main/kotlin/com/waryway/gab/ui/LocalLlmSendUx.kt` (only if Local gaps proven)
- New pure helper under `src/main/kotlin/com/waryway/gab/ui/` if needed (e.g. `GrokBuildSendUx.kt`)
- `src/test/kotlin/com/waryway/gab/ui/LocalLlmSendUxTest.kt`
- New test file under `src/test/kotlin/com/waryway/gab/ui/` for Grok Build UX helpers
- Read-only reference: `GrokBuildAuth.kt`, `WarywayGabSettings.kt` (session summary APIs)

**Do not touch:**

- `WarywayGabToolWindowPanel.kt` / `LocalLlmWorkbenchPanel.kt` (owned by wo-04-02)
- Stream merge / SSE (`StreamContentMerger`, `StreamUiCoalescer`, `GabSseAccumulator`)
- Agent poll loop (`LocalLlmAgentSession`, `AgentClient`)
- Version / CHANGELOG / README ship notes (section-05)
- Auth.json parser rewrite (section-01)

## Commands (run from repo root)

```bash
./gradlew.bat test --tests "com.waryway.gab.ui.*"
```

If filter fails, run full `./gradlew.bat test` and report scope.

## Acceptance

- [x] Pure recovery formatter(s) exist for Grok Build missing/expired/401-class/network cases (or explicitly reuse section-01 helper with tests covering UX strings)
- [x] No duplicate parallel message catalog vs section-01
- [x] Local LLM send path labels unchanged unless a bug is proven (`Chat` | `Agent · dry-run` | `Agent · APPLY`)
- [x] Unit tests green for new/changed pure UX helpers
- [x] No Swing code in pure helpers

## Worker report

**Status:** done  

**Files changed:**

- `src/main/kotlin/com/waryway/gab/ui/GrokBuildSendUx.kt` (new — pure send-path UX; auth strings delegate to `GrokBuildAuthRecovery`)
- `src/test/kotlin/com/waryway/gab/ui/GrokBuildSendUxTest.kt` (new — delegation + network + `formatFailure` coverage)

**Summary:**

Added pure `GrokBuildSendUx` for Grok Build send failures: thin wrappers over section-01 `GrokBuildAuthRecovery` for missing/expired/401–403 recovery (no forked string catalog), plus SendUx-owned cli-chat-proxy network copy and `formatFailure` mapping (auth → network → generic `Error:`). Local LLM labels/helpers untouched. Unit tests green for `GrokBuildSendUxTest`, `LocalLlmSendUxTest`, and `GrokBuildAuthRecoveryTest`.

**Blockers:**

- None

**Tests run:**

```
./gradlew.bat test --tests com.waryway.gab.ui.GrokBuildSendUxTest --tests com.waryway.gab.ui.LocalLlmSendUxTest --tests com.waryway.gab.client.GrokBuildAuthRecoveryTest
./gradlew.bat test --tests com.waryway.gab.ui.*
```

Both BUILD SUCCESSFUL.
