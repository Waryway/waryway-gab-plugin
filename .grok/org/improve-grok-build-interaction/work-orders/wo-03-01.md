# Work Order — wo-03-01

**Order ID:** wo-03-01  
**Section:** section-03-agent-api  
**Iteration:** `.grok/org/improve-grok-build-interaction/iterations/section-03-agent-api/iteration-01.md`  
**Status:** done  
**Worker role:** implementer

## Task

Harden **terminal finish content and status strings** for the Local LLM agent path so every terminal outcome produces non-blank user-visible final content and sensible status text. Prefer extracting pure helpers (companion or small object) from `LocalLlmAgentSession.finish` / `statusLine` so behavior is unit-testable without HTTP.

Cover at least:

1. **done + finalAnswer** — preserves answer; dry-run vs APPLY badge text present (existing behavior preserved or improved)
2. **done + empty finalAnswer** — not a blank success bubble (placeholder or explicit “no final answer” with state)
3. **failed + error** — `Error: …` content; status indicates failed
4. **cancelled / userStopped** — non-blank “Stopped by user.” (or equivalent); status cancelled when state is cancelled
5. **failed empty error / odd state** — still non-blank content (never blank finalContent)
6. **statusLine / terminal onStatus** — dry-run vs APPLY reflected in planning and done statuses

Wire `finish` to the pure helpers. Do **not** redesign the poll loop in this order (that is wo-03-02). Do **not** route GROK_BUILD or cloud into `/api/agent`.

Expand `LocalLlmAgentSessionTest` with pure-function tests for the cases above (keep existing attachment/goal tests green).

## Scope

**Edit only:**

- `src/main/kotlin/com/waryway/gab/chat/LocalLlmAgentSession.kt`
- `src/test/kotlin/com/waryway/gab/chat/LocalLlmAgentSessionTest.kt`
- Optional new pure helper file under `src/main/kotlin/com/waryway/gab/chat/` if extraction needs a dedicated type (e.g. `LocalLlmAgentTerminal.kt`) + matching test only if cleaner than companion

**Do not touch:**

- `AgentClient.kt` (wo-03-02 may touch lightly; leave HTTP client alone unless a one-line visibility change is required for pure helpers — prefer not)
- `WarywayGabToolWindowPanel.kt` (section-04 / only if cancel glue broken — out of this order)
- StreamContentMerger, GrokBuildAuth, workbench layout, version/CHANGELOG
- Cloud `AgentSession`

## Commands (run from repo root)

```bash
./gradlew.bat test --tests "com.waryway.gab.chat.LocalLlmAgentSessionTest"
```

## Acceptance

- [x] Pure helpers (or package-visible API) exist for final content + terminal status formatting
- [x] Empty `finalAnswer` + non-error terminal does not yield blank `finalContent`
- [x] dryRun true/false reflected in status and final badge text
- [x] Unit tests cover done / failed / cancelled / user-stop / empty-finalAnswer cases
- [x] Existing attachment/goal tests still pass
- [x] No GROK_BUILD/cloud routing changes into `/api/agent`

## Worker report

**Status:** done  
**Files changed:**

- `src/main/kotlin/com/waryway/gab/chat/LocalLlmAgentSession.kt`
- `src/test/kotlin/com/waryway/gab/chat/LocalLlmAgentSessionTest.kt`

**Summary:**

Extracted pure companion helpers `formatFinalContent`, `formatTerminalStatus`, `formatStatusLine`, and `formatPlanningStatus` (plus badge/stop constants) from the Local LLM agent session finish/status path. Wired `finish`, poll `statusLine`, and start planning `onStatus` to those helpers so every terminal outcome yields non-blank content and dry-run vs APPLY badges/status. Expanded `LocalLlmAgentSessionTest` with 17 pure-function cases covering done/failed/cancelled/user-stop/empty-finalAnswer/odd-state; full suite **30 tests, 0 failures**.

**Blockers:**

- None

**Command result:**

```
./gradlew.bat test --tests "com.waryway.gab.chat.LocalLlmAgentSessionTest" --no-build-cache
# BUILD SUCCESSFUL — TEST-com.waryway.gab.chat.LocalLlmAgentSessionTest.xml tests=30 failures=0 errors=0
```
