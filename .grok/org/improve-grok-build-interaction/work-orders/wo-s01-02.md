# Work Order — wo-s01-02

**Order ID:** wo-s01-02  
**Section:** section-01-auth-session  
**Iteration:** `.grok/org/improve-grok-build-interaction/iterations/section-01-auth-session/iteration-01.md`  
**Status:** done  
**Worker role:** implementer

## Task

Surface **Grok Build session refresh and auth recovery** in Settings UI and minimal tool-window hooks so operators recover without full IDE restart.

### Dependency on wo-s01-01

Use pure APIs from **wo-s01-01**:

- `WarywayGabSettings.grokBuildSessionSummary()` / any `refreshGrokBuildSession()`
- `com.waryway.gab.client.GrokBuildAuthRecovery` (or final name from wo-s01-01)

If the recovery helper is not on disk yet when you start (concert race): implement against the **contract documented in wo-s01-01**; create only a minimal stub file if required to compile, and leave full pure tests to wo-s01-01. Do **not** invent a second long-lived string table in Swing code.

### 1. `WarywayGabConfigurable` — Grok Build group

Today the session summary `comment(...)` is captured when the panel is built (stale until reopen).

- Add a **Refresh session** (or equivalent) button that re-reads live session and updates the visible summary (label/comment control you can rebind, or a `JBLabel` you set text on).
- Keep existing Test Connection + auth path comment.
- Help text should note: live `~/.grok/auth.json` is preferred; optional PasswordSafe override is fallback when live session is missing/unusable (align with settings KDoc from wo-s01-01).
- After refresh, summary must show missing vs expired distinctly when those states apply.

### 2. `WarywayGabToolWindowPanel` — minimal auth hooks only

**Edit only** auth/session-related regions. Do **not** rewrite stream flush, Local LLM workbench, send-path badge styling, or agent cancel (other sections).

Required:

1. **Provider switch / first-run coaching** (existing GROK_BUILD branches around `onProviderChanged`, coaching steps, empty-key dialogs): ensure missing vs expired are distinct when `hasApiKey` / summary is used. Prefer `settings.grokBuildSessionSummary()` / recovery helper over hard-coded one-liners that blur expired into "no session".
2. **Send / chat failure path** for `ModelProvider.GROK_BUILD` (today ~`Error: ${e.message}` for non-Local providers): when failure is auth-class, show recovery copy from `GrokBuildAuthRecovery.formatAuthFailure(...)` (or settings wrapper). Keep Local LLM on `LocalLlmSendUx.formatFailure`. Do not change non-auth GROK_BUILD error presentation beyond optional thin prefix if needed for consistency.
3. **Optional:** lightweight "refresh session" after successful detection that auth improved is **not** required if every Send already re-reads credentials via `createClient`/`getApiKey` (verify live read). If any path caches the API key for the whole IDE session, fix **only** that cache for GROK_BUILD so next Send picks up new `auth.json`.

### 3. Tests

UI integration tests are **not** required. If you extract a tiny pure helper for message selection, unit-test it; otherwise rely on wo-s01-01 tests.

## Scope

**Edit only:**

- `src/main/kotlin/com/waryway/gab/settings/WarywayGabConfigurable.kt`
- `src/main/kotlin/com/waryway/gab/ui/WarywayGabToolWindowPanel.kt` (**auth/session coaching + GROK_BUILD error mapping regions only**)
- Only if compile requires and wo-s01-01 not landed: thin stub of recovery API (prefer waiting / coordinating via contract)

**Do not touch:**

- `GrokBuildAuth.kt` / recovery helper implementation (wo-s01-01) except stub if absolutely required
- Stream merge, SSE, `GabClient` protocol rewrite (section-02 may own client; only call recovery from UI when mapping exceptions — do not change GabClient throw format unless a one-line status pass-through is essential; prefer parsing `e.message` / `GabApiException` in the panel)
- Local LLM workbench / send badge (section-04)
- Version / CHANGELOG (section-05)

## File-region discipline (panel)

Safe touch points (by current structure):

- `onProviderChanged` GROK_BUILD hints
- Coaching steps `ModelProvider.GROK_BUILD` branch
- Empty-state "Use Grok Build" / "Test Key" dialogs that mention login
- Catch block that formats `errorText` for chat Send (`LOCAL_LLM` vs else)

Avoid: `refreshSendPathBadge`, stream flush timers, agent run path, attachment chips, model list plumbing except if a model-refresh error is clearly 401 for GROK_BUILD and one-line recovery is trivial.

## Commands (run from repo root)

```bash
./gradlew.bat test --tests "com.waryway.gab.client.GrokBuildAuth*"
```

Compile check if no new tests:

```bash
./gradlew.bat testClasses
```

or full:

```bash
./gradlew.bat test
```

## Acceptance

- [x] Settings Grok Build group can refresh session summary without reopening IDE / full settings rebuild as sole path
- [x] Missing vs expired coaching visible in settings and/or tool window with actionable `grok login` + path
- [x] GROK_BUILD chat 401/403-class failures show recovery copy (not bare stack-style only)
- [x] After `grok login` updates `auth.json`, next Send or explicit refresh uses live session (no full IDE restart)
- [x] No drive-by edits to Local LLM / stream / agent regions
- [x] Build green for scoped checks

## Worker report

**Status:** done  

**Files changed:**

- `src/main/kotlin/com/waryway/gab/settings/WarywayGabConfigurable.kt` — live `JBLabel` session summary, **Refresh session** button → `refreshGrokBuildSession()`, live-vs-override help copy, Test Connection uses recovery for missing/expired + auth-class failures
- `src/main/kotlin/com/waryway/gab/ui/WarywayGabToolWindowPanel.kt` — first-run/provider coaching uses `grokBuildSessionSummary()` / recovery helpers for missing vs expired (Use Grok Build, Test Key, empty paste, pre-send, `onProviderChanged`); chat catch already routes GROK_BUILD via `GrokBuildChatFailureUx` (parallel wo-02/04 landing)

**Summary:**

Settings Grok Build group no longer freezes the session summary at panel-build time: a `JBLabel` is updated by **Refresh session** and after Test Connection success/failure via `WarywayGabSettings.refreshGrokBuildSession()` / `grokBuildSessionSummary()` (recovery string table for missing vs expired). Help text documents live `auth.json` preference over PasswordSafe override. Tool-window empty-state and no-key paths use the same summary/recovery copy; Send failures for GROK_BUILD map through `GrokBuildChatFailureUx` → `GrokBuildSendUx` → `GrokBuildAuthRecovery`. Credentials remain live-read (`grokBuildAccessToken` / no session cache). Scoped compile + auth UX unit tests green.

**Blockers:**

- None

**Commands run:**

- `./gradlew.bat compileKotlin --no-daemon` — BUILD SUCCESSFUL
- `./gradlew.bat testClasses --no-daemon` — BUILD SUCCESSFUL
- `./gradlew.bat test --tests "com.waryway.gab.client.GrokBuildAuth*" --tests "com.waryway.gab.ui.GrokBuildSendUx*" --tests "com.waryway.gab.ui.GrokBuildChatFailureUx*" --no-daemon` — BUILD SUCCESSFUL (GrokBuildAuthRecoveryTest 12, GrokBuildAuthTest 7, GrokBuildChatFailureUxTest 11, GrokBuildSendUxTest 20; 0 failures)

**Residuals for other WOs:**

- Panel `refreshSendPathBadge` GROK_BUILD chip + click-to-refresh was already present from parallel work (section-04 / wo-02); this WO did not re-own that styling.
- Chat failure mapping lives in `GrokBuildChatFailureUx` (wo-02-02); panel catch already uses it — no second string table added.
