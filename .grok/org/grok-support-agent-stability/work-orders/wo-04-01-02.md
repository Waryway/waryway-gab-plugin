# Work Order — wo-04-01-02

**Order ID:** wo-04-01-02  
**Section:** section-04-regression-tests  
**Iteration:** `.grok/org/grok-support-agent-stability/iterations/section-04-regression-tests/iteration-01.md`  
**Status:** done  
**Worker role:** tester

## Task

Extend **SSE / tool-call / finish-reason** unit tests so premature-stop failure modes (empty stream, silent error, dropped incomplete tools, finish_reason loss) cannot regress.

### Soft dependency (section-02)

1. **Discover first:** Read `GabSseAccumulator.kt`, `GabClient.ChatCompletionResult`, and any new fields/flags from section-02 / **wo-s02-01-01** (e.g. incomplete-tool signal, stream error on result, cancel distinction).
2. **Coordinate with wo-s02-01-02:** section-02 already owns SSE fidelity unit tests. This order **extends/regression-locks** the matrix (empty stream, error non-silent, finish_reason variants, incomplete builders) rather than duplicating identical cases. If wo-s02-01-02 already covers a case and is green, keep it and add only complementary assertions or document overlap in the report.
3. Prefer asserting **landed** behavior. If production still silently drops incomplete tools or treats error events as empty success, tests should **fail loudly** on that behavior **only if** section-02 already claims a fix; otherwise lock the **target contract** from section-02 ticket and note “awaits section-02” if you cannot fix production (production SSE fixes are **out of scope** for this order — tests only).

### Required cases

| Case | Expected |
|------|----------|
| Empty stream (no data / only `[DONE]`) | Distinguishable empty result; finishReason null or documented; not fabricated success content |
| SSE `error` event via `processSseLine` | Event is `SseEvent.Error` with message (existing test may already cover); extend if result path should fail or flag error when section-02 adds mapping |
| `finish_reason` variants: `tool_calls`, `stop`, `length`, absent/null | Captured consistently on `ChatCompletionResult`; null vs `"stop"` distinguishable |
| Incomplete tool builder at end (blank id and/or name) | **Not** silent empty tools without signal — assert recovery **or** incomplete/error flag per section-02; if still `mapNotNull` drop-only, assert current behavior and file blocker note for section-02 gap |
| Existing fragmented tool_calls happy path | Remains green |

Preserve existing tests in `GabSseAccumulatorTest.kt`; add cases rather than rewrite.
Fixtures: inline SSE `data:` line sequences only — no network.

## Scope

**Edit only:**

- `src/test/kotlin/com/waryway/gab/client/GabSseAccumulatorTest.kt`
- Optionally `src/test/kotlin/com/waryway/gab/client/GabClientJsonTest.kt` if non-stream finish/tool parse assertions fit better there
- Production files: **none** unless section-02 already exposed package-private test hooks you must call (prefer companion/public test entry points already used)

**Do not touch:**

- `AgentSession` loop policy (wo-04-01-01)
- Model catalog / Grok settings (wo-04-01-03)
- Live client HTTP code paths beyond pure parse helpers already under test

## Commands (run from repo root)

```bash
./gradlew test --tests "com.waryway.gab.client.GabSseAccumulatorTest" --tests "com.waryway.gab.client.GabClientJsonTest"
```

## Acceptance

- [x] Empty stream covered
- [x] Error event non-silent (event and/or result path per landed API)
- [x] finish_reason variants covered
- [x] Incomplete tool_call builders covered with explicit expectation
- [x] Existing SSE tests still green
- [x] No network in unit tests

## Worker report

**Status:** done  
**Files changed:**

- `src/test/kotlin/com/waryway/gab/client/GabSseAccumulatorTest.kt`

**Summary:**

Coordinated with wo-s02-01-02: base matrix (empty stream, error → streamError, finish_reason tool_calls/stop/length/null/absent, incomplete builders, cancel, fragmented tools) was already landed; preserved all existing cases. Added complementary premature-stop regression locks: Finish event callback, error-after-partial-content dual signal, null-vs-stop distinguishability, empty `data:` payloads, and incomplete-tools+streamError together. Asserted landed ChatCompletionResult fields (`streamError`, `incompleteToolCallCount`, `cancelled`, `finishReason`). `./gradlew test --tests GabSseAccumulatorTest --tests GabClientJsonTest` green (32 tests).

**Blockers:**

- None
