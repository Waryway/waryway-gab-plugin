# Work Order — wo-01-03

**Order ID:** wo-01-03  
**Section:** section-01-grok-provider  
**Iteration:** `.grok/org/grok-support-agent-stability/iterations/section-01-grok-provider/iteration-01.md`  
**Status:** done  
**Worker role:** implementer

## Task

Polish **Grok settings/UX and onboarding coaching** so Grok is clearly first-class and never mis-described as Gab-only.

1. **`WarywayGabConfigurable` — “Grok (xAI)” group**
   - API key field binds to `ModelProvider.GROK` only
   - Default model binds to Grok default model state
   - Comments mention `console.x.ai` / recommended Grok model ids (sync with `ModelCatalog` if catalog ids change in wo-01-01)
   - **Test Grok Connection** uses `createClient(GROK)` + models list (or lightweight probe) and filters with `ModelCatalog.filterForProvider(..., GROK)`
   - Error/success copy names Grok correctly (not “Gab” generically when testing Grok)

2. **`WarywayGabToolWindowPanel` routing / coaching**
   - Provider selector and coaching steps for GROK remain accurate (console.x.ai, API keys, quota)
   - Key test path for Grok stores key under GROK and creates Grok client
   - No user-visible string that implies Grok uses Gab AI credentials or gab.ai endpoints
   - Tooltip/copy that says “Gab AI or Grok” should remain balanced if present

3. **Consistency**
   - Prefer consistent product naming: settings group “Grok (xAI)”, provider displayName “Grok”, help URL console.x.ai
   - Do **not** redesign the UI or add new settings pages — fix accuracy only

4. **Tests**
   - Pure UI is hard to unit-test; if you extract a tiny pure helper for connection messaging or provider labels, test it. Otherwise document manual checks in Worker report:
     - Grok group fields bind to GROK
     - Test Connection message uses provider.displayName
     - Coaching HTML for GROK points to console.x.ai

## Scope

**Edit only:**

- `src/main/kotlin/com/waryway/gab/settings/WarywayGabConfigurable.kt`
- `src/main/kotlin/com/waryway/gab/ui/WarywayGabToolWindowPanel.kt`
- Optional: `src/main/resources/messages/WarywayGabBundle.properties` if Grok strings live there

**Do not touch:**

- Agent loop / SSE internals
- Local LLM agent server protocol
- Full UI redesign / new pages
- Catalog model id changes (coordinate with wo-01-01; only update comments to match)

## Investigation checklist

- [x] Test Connection path for Grok: models list or lightweight probe? (document which)
- [x] Coaching mentions Grok correctly where relevant
- [x] Switching provider does not leave wrong key field wired

## Commands (run from repo root)

Compile check at minimum:

```bash
./gradlew.bat test --tests "com.waryway.gab.model.ModelCatalogTest" --tests "com.waryway.gab.client.GabClientJsonTest"
```

Or full:

```bash
./gradlew.bat test
```

## Acceptance

- [x] Settings UI “Grok (xAI)” group: API key, default model, Test Connection accurate
- [x] Coaching/copy mentions Grok correctly where relevant
- [x] No silent wrong-provider messaging or key wiring
- [x] No unrelated UI redesign
- [x] Build green for compile + related tests

## Worker report

**Status:** done  
**Files changed:**

- `src/main/kotlin/com/waryway/gab/settings/WarywayGabConfigurable.kt`
- `src/main/kotlin/com/waryway/gab/ui/WarywayGabToolWindowPanel.kt`
- `src/main/resources/messages/WarywayGabBundle.properties`

**Summary:**

Grok settings group already bound API key / default model / Test Connection to `ModelProvider.GROK` via `createClient` + `listModels` + `filterForProvider`. Polished accuracy: connection dialogs name the provider (`Grok connection` / failures include displayName), recommended model comment derives from `ModelCatalog.fallbackModelIds(GROK)`, coaching stresses Grok keys are not Gab AI credentials and points at console.x.ai, provider tooltip lists Local LLM / Grok / Gab AI, and bundle coaching/settings strings no longer read as Gab-only.

**Blockers:**

- None for this WO. Note: `ModelCatalogTest.sortForDisplay puts grok models first for grok provider` failed once (catalog filter/sort assertion vs `gpt-5` in GROK list) — out of edit scope; owned by wo-01-01. `compileKotlin` / `compileTestKotlin` green; GabClientJsonTest suite green.

## Manual verification notes (worker fills)

- Test Connection behavior: Settings **Test Grok Connection** → `settings.setApiKey(key, GROK)` → `createClient(GROK)` → `listModels()` → `ModelCatalog.filterForProvider(..., GROK)`; success/error titles and bodies use `provider.displayName` (e.g. “Grok connection successful!” / “Grok connection failed: …”). Same probe pattern for coaching **Test Key**.
- Coaching URLs checked: GROK steps link to `https://console.x.ai`; GAB_AI steps stay on `https://gab.ai`; Local LLM healthz unchanged. Hyperlink fallback uses `provider.keyHelpUrl`.
- Strings fixed: Configurable Grok key comment (console.x.ai / Grok-only storage); recommended models synced to catalog fallbacks; tooltip “Provider (Local LLM, Grok, or Gab AI)”; GROK coaching step 4 clarifies not Gab AI; GAB coaching notes separate from Grok; bundle coaching + settings key help multi-provider.
