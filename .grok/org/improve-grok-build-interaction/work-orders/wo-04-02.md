# Work Order — wo-04-02

**Order ID:** wo-04-02  
**Section:** section-04-ux-recovery  
**Iteration:** `.grok/org/improve-grok-build-interaction/iterations/section-04-ux-recovery/iteration-01.md`  
**Status:** done  
**Worker role:** implementer

## Task

Wire **tool window** and **Local LLM workbench** so send paths and failures guide recovery, and session/status is visible on the main Grok Build path.

### Requirements

1. **Send failure formatting (GROK_BUILD)**  
   In `WarywayGabToolWindowPanel` chat Send catch path (today bare `"Error: ${e.message}"` for non–Local-LLM), use pure formatters from wo-04-01 / section-01 for GROK_BUILD auth/proxy/network classes. Keep Local LLM on `LocalLlmSendUx.formatFailure`. Other providers may keep generic error if no formatter exists.

2. **Pre-send / no-key coaching**  
   Align missing-session coaching strings with the pure helper (provider change + Send gate) so operators always see the same recovery path (`grok login`, auth file).

3. **Send path badge (Local LLM)**  
   Keep `refreshSendPathBadge()` Local-LLM-only with labels/tooltips from `LocalLlmSendUx` (`Chat` | `Agent · dry-run` | `Agent · APPLY`). Do **not** show agent dry-run/APPLY badges for GROK_BUILD or other providers.

4. **Workbench truthfulness**  
   Confirm `LocalLlmWorkbenchPanel` is only mounted for Local LLM (or otherwise cannot claim agent mode for non–Local providers). Fix if any leakage exists. Path badges/status lines stay driven by `LocalLlmSendUx`.

5. **Optional Grok Build session affordance**  
   Add a light persistent status for GROK_BUILD in the main path — e.g. reuse/send-path badge area with session summary text, a system line on focus, or a small refresh control that re-reads `grokBuildSessionSummary()` / live session without full provider re-select. Prefer minimal UI; no theme redesign.

6. **Depends on pure helpers**  
   Prefer wo-04-01 landing first for new formatters. If helpers are missing when you start, implement a minimal pure helper in `ui/` yourself **only if** section-01 has not added one yet — still single catalog, document in worker report.

### File regions to prefer in tool window

- `onProviderChanged` coaching
- `refreshSendPathBadge`
- Send preflight no-key hint
- Chat catch `formatFailure` branch (~request failed)
- Any existing system-line session summary hooks

Avoid rewriting stream flush, conversation tabs, or agent worker machinery.

## Scope

**Edit only:**

- `src/main/kotlin/com/waryway/gab/ui/WarywayGabToolWindowPanel.kt` (badge, failure formatting, light session status, coaching — file-scoped regions)
- `src/main/kotlin/com/waryway/gab/ui/LocalLlmWorkbenchPanel.kt` (only if needed for truthfulness/labels)
- Pure helpers under `src/main/kotlin/com/waryway/gab/ui/*Ux*.kt` **only if** wo-04-01 not yet available and no section-01 formatter exists
- Tests only if you add pure logic here (prefer wo-04-01 owns pure tests)

**Do not touch:**

- `StreamContentMerger` / `StreamUiCoalescer` / SSE accumulator (section-02)
- `LocalLlmAgentSession` / `AgentClient` poll loop (section-03)
- `GrokBuildAuth` parser internals (section-01) — call public APIs only
- Version / CHANGELOG (section-05)
- ActivityLog, attachments panels, full settings redesign

## Commands (run from repo root)

```bash
./gradlew.bat test --tests "com.waryway.gab.ui.*"
```

Compile-check at minimum if full suite is slow:

```bash
./gradlew.bat compileKotlin compileTestKotlin
```

## Acceptance

- [x] GROK_BUILD Send failures use recovery-oriented pure formatters (not bare `Error: …` for auth/proxy classes when formatter exists)
- [x] Missing-session coaching consistent with pure helper
- [x] Local LLM send path labels/tooltips/status remain correct; badge still Local-only
- [x] Workbench does not claim agent mode for non–Local-LLM providers
- [x] Optional session status affordance present **or** explicitly deferred with justification if blocked (prefer implement if low cost)
- [x] Build/tests for touched packages green or failures documented

## Worker report

**Status:** done  

**Files changed:**

- `src/main/kotlin/com/waryway/gab/ui/WarywayGabToolWindowPanel.kt` — Send catch via `GrokBuildChatFailureUx` → `GrokBuildSendUx.formatFailure`; pre-send + provider-change coaching via `grokBuildPreSendCoaching()` / pure helpers; `refreshSendPathBadge` Local-LLM agent labels only + GROK_BUILD session chip (click → `refreshGrokBuildSession`); workbench still LOCAL_LLM-only mount
- (read/reuse, no edit required) `GrokBuildSendUx.kt`, `GrokBuildChatFailureUx.kt`, `GrokBuildAuthRecovery.kt`, `LocalLlmWorkbenchPanel.kt`

**Summary:**

Wired the main tool-window Send path so GROK_BUILD failures use pure recovery formatters (`GrokBuildChatFailureUx` → `GrokBuildSendUx` / `GrokBuildAuthRecovery`) instead of bare `Error: ${e.message}`. Missing/expired session coaching is unified on provider change and Send preflight. Local LLM still owns agent dry-run/APPLY badge labels; workbench remains mounted only for LOCAL_LLM. Added a light clickable Grok Build session chip on the send-path badge (`Grok · email` / `Session expired` / `No session`) that re-reads auth without provider re-select.

**Blockers:**

- None

**Tests run:**

```
./gradlew.bat test --tests "com.waryway.gab.ui.*" --no-daemon
```

BUILD SUCCESSFUL (ui package tests green).
