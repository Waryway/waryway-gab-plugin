# Work Order — wo-04-01-03

**Order ID:** wo-04-01-03  
**Section:** section-04-regression-tests  
**Iteration:** `.grok/org/grok-support-agent-stability/iterations/section-04-regression-tests/iteration-01.md`  
**Status:** done  
**Worker role:** tester

## Task

Lock **Grok (xAI) catalog/provider selection** and **GROK chat request construction** contracts with unit tests so provider routing and request shape cannot silently regress.

### Soft dependency (section-01)

1. **Discover first:** `ModelCatalog.kt`, `ModelProvider.kt`, `GabClient.buildJsonChatRequest`, and any section-01 request quirks (omit Gab-only fields, tools/stream defaults for GROK).
2. Extend existing tests; do not delete coverage for Gab/Local LLM.

### Required cases

| Case | Expected |
|------|----------|
| Grok default model + fallbacks | `GROK_DEFAULT_MODEL_ID` / first fallback still product intent (currently `grok-4.3` unless section-01 documented change) |
| `belongsToProvider` / `isGrok` / sortForDisplay for GROK | Grok models kept/ordered; Gab models not treated as Grok defaults |
| `resolveSelection` for GROK | Prefers provider default when selection missing |
| `buildJsonChatRequest` with `GabClient(..., ModelProvider.GROK)` | Valid OpenAI-compatible JSON: model, messages, `stream` when requested, tools/`tool_choice` when `includeTools`+toolsJson provided; no Local-LLM-only `localllm` preset blob |
| Provider base URL constant (if easily assertable without network) | `ModelProvider.GROK.baseUrl` is `https://api.x.ai/v1` (or landed value) |

Do **not** call live xAI. Use pure catalog functions and `GabClient` JSON builders already unit-tested style.

## Scope

**Edit only:**

- `src/test/kotlin/com/waryway/gab/model/ModelCatalogTest.kt`
- `src/test/kotlin/com/waryway/gab/client/GabClientJsonTest.kt`
- Optional: small new test file under `src/test/kotlin/com/waryway/gab/model/` for `ModelProvider` constants if cleaner

**Do not touch:**

- Production catalog/client unless a trivial `internal` visibility is already the project pattern (prefer tests only)
- AgentSession / SSE parser
- Settings UI

## Commands (run from repo root)

```bash
./gradlew test --tests "com.waryway.gab.model.ModelCatalogTest" --tests "com.waryway.gab.client.GabClientJsonTest"
```

## Acceptance

- [x] Catalog assertions cover Grok default, belongsToProvider/sort, resolveSelection
- [x] GROK `buildJsonChatRequest` tools/stream shape asserted
- [x] No network dependency
- [x] Existing catalog + JSON tests remain green

## Worker report

**Status:** done  

**Files changed:**

- `src/test/kotlin/com/waryway/gab/model/ModelCatalogTest.kt`
- `src/test/kotlin/com/waryway/gab/client/GabClientJsonTest.kt`

**Summary:**

Extended ModelCatalogTest with stronger filterForProvider regression asserts (GROK list never includes gpt-5 / non-Grok models) and a dedicated belongsToProvider/isGrok test that keeps Gab defaults out of Grok. Extended GabClientJsonTest with three pure-unit GROK request-shape cases: OpenAI-compatible model/messages/stream without localllm/max_tokens; tools + tool_choice auto when includeTools+toolsJson; tools omitted when includeTools=false. Preserved all wo-01-01 catalog coverage and existing Gab/Local JSON tests. `clean test` for both suites green (25 tests).

**Blockers:**

- None
