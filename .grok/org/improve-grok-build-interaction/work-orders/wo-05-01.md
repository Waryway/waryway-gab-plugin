# Work Order — wo-05-01

**Order ID:** wo-05-01  
**Section:** section-05-regression-ship  
**Iteration:** `.grok/org/improve-grok-build-interaction/iterations/section-05-regression-ship/iteration-01.md`  
**Status:** done  
**Worker role:** implementer

## Task

Close **cross-cutting pure unit test gaps** for Grok Build interaction changes landed in sections 01–04. Prefer audit-first: if coverage already locks the acceptance surfaces, document that and add only thin missing guards — do not invent brittle full-IDE UI tests or product features.

### 1. Audit (required first step)

Confirm pure/unit coverage exists for each AC surface (read tests + source; do not re-implement product logic):

| Surface | Primary types | Expected existing tests |
|---------|---------------|-------------------------|
| Stream merge (guard) | `StreamContentMerger`, `StreamUiCoalescer`, SSE accumulator | `StreamContentMergerTest`, `StreamUiCoalescerTest`, `GabSseAccumulatorTest` |
| Grok Build auth usable / expired / recovery | `GrokBuildAuth`, `GrokBuildAuthRecovery` | `GrokBuildAuthTest`, `GrokBuildAuthRecoveryTest` (`classifySession`, missing/expired coaching, 401/403 map) |
| Local LLM send UX | `LocalLlmSendUx` | `LocalLlmSendUxTest` (path labels + formatFailure) |
| Agent terminal / cancel helpers | `LocalLlmAgentSession` pure finish + `AgentRunOps` cancel/timeout | `LocalLlmAgentSessionTest` (format + FakeAgentOps), `AgentClientTest` terminal parse |
| Grok Build send/chat failure UX | `GrokBuildSendUx`, `GrokBuildChatFailureUx` | `GrokBuildSendUxTest`, `GrokBuildChatFailureUxTest` |

Baseline verifier (sections 01–04): **268** scoped tests green under `client.*` / `chat.*` / `ui.*` / `model.*` (`delegations/verifier-report-iter-01.md`).

### 2. Fill only real thin gaps

Add pure unit tests **only** when audit shows a missing contract that is unit-testable without Swing/IDE. Priority candidates called out by prior managers (optional if already covered):

1. **`GrokBuildAuth.requestHeaders(accessToken, modelForOverride)`** — assert map contains Authorization / `X-XAI-Token-Auth` / client version-surface headers; with non-blank model includes override header; blank/null model omits override. (Section-02 residual; pure map, no HTTP.)
2. Any **missing pure case** on terminal helpers / recovery classifiers discovered during audit (e.g. empty-finalAnswer, user-stop, non-auth `formatAuthFailure` → null) **only if not already asserted**.
3. Do **not** add full-panel UI tests, live HTTP against cli-chat-proxy, or mock-server e2e.

If audit finds **no gaps**, write that conclusion in the Worker report with evidence (test class + method names) and leave sources unchanged.

### 3. Constraints

- Tests only (or tiny pure test-helper visibility if a pure function is package-private and already intended for tests — avoid public API expansion).
- **No** product feature work, panel rewrites, stream redesign, or dependency upgrades.
- **No** version / CHANGELOG / README edits (owned by **wo-05-02**).
- Prefer extending existing `*Test.kt` files over new suites unless a new pure type needs isolation.

## Scope

**Edit only (as needed):**

- `src/test/kotlin/com/waryway/gab/client/**` (e.g. `GrokBuildAuthTest.kt`, stream/auth suites)
- `src/test/kotlin/com/waryway/gab/chat/**` (e.g. `LocalLlmAgentSessionTest.kt`)
- `src/test/kotlin/com/waryway/gab/ui/**` (only if a pure UX case is truly uncovered)
- Main sources **only** if a pure helper must become `internal`/`@VisibleForTesting` to assert without reflection — keep minimal

**Do not touch:**

- `gradle.properties`, `CHANGELOG.md`, `README.md`, `HOW_TO_LOAD_IN_IDE.md` (wo-05-02)
- `WarywayGabToolWindowPanel.kt` / Configurable / workbench layout
- Stream merger redesign, AgentSession cloud loop, marketplace/CI
- Unrelated packages (`tools`, `skills`, `diagnostics`) unless a one-line pure gap is in-scope and already part of 01–04 delta (prefer skip)

## Commands (run from repo root)

```bat
./gradlew.bat test --tests "com.waryway.gab.client.*" --tests "com.waryway.gab.chat.*" --tests "com.waryway.gab.ui.*" --tests "com.waryway.gab.model.*"
```

If you add tests under other packages, include them in the filter and report. Prefer `--no-daemon` if Kotlin daemon contention appears.

## Acceptance

- [x] Audit completed and documented in Worker report (table: surface → suite → covered Y/N)
- [x] Missing pure unit cases filled **or** explicit “no gaps” with evidence
- [x] New/changed tests are pure/unit-style (no brittle full-IDE UI tests)
- [x] Scoped suite green for packages touched
- [x] No product features, version/docs, or unrelated refactors

## Worker report

**Status:** done  

**Files changed:**

- `src/test/kotlin/com/waryway/gab/client/GrokBuildAuthTest.kt` — +3 pure `requestHeaders` cases; surface/User-Agent constants asserted

**Summary:**

Audit-first pass over sections 01–04 pure surfaces. All AC tables already locked except the known residual `GrokBuildAuth.requestHeaders` map contract (section-02). Added three pure map asserts (auth headers + model override include/omit/trim). Terminal/recovery/UX suites already cover empty-finalAnswer, user-stop, non-auth `formatAuthFailure → null`. No product, version, or docs edits.

**Audit table (surface → suite → covered):**

| Surface | Suite(s) | Covered | Evidence (sample methods) |
|---------|----------|---------|---------------------------|
| Stream merge (guard) | `StreamContentMergerTest` (10), `StreamUiCoalescerTest` (9), `GabSseAccumulatorTest` (29) | Y | `normal deltas append`, `snapshot extension replaces`, `cumulative snapshot chunks do not quadratic append`, `offer accumulates without force until max` |
| Grok Build auth usable/expired/recovery | `GrokBuildAuthTest` (10), `GrokBuildAuthRecoveryTest` (12) | Y | `classifySession maps missing expired usable`, `session isExpired applies 60s near-expiry skew`, `formatAuthFailure maps HTTP 401/403`, `formatAuthFailure returns null for non-auth HTTP errors`, **+** `requestHeaders includes Authorization TokenAuth version surface and User-Agent` |
| Local LLM send UX | `LocalLlmSendUxTest` (14) | Y | `sendPathLabel agent dry-run/APPLY/chat`, `formatFailure connection uses offline guidance`, `formatFailure non-connect keeps Error prefix` |
| Agent terminal / cancel | `LocalLlmAgentSessionTest` (35), `AgentClientTest` (12) | Y | `formatFinalContent done empty finalAnswer is not blank`, `userStopped keeps non-blank finalAnswer`, `run mid-run cancelActiveRun finishes`, `run timeout cancels server run`, `isTerminal true only for done failed cancelled` |
| Grok Build send/chat failure UX | `GrokBuildSendUxTest` (20), `GrokBuildChatFailureUxTest` (11) | Y | `formatAuthFailure returns null for non-auth`, `formatFailure auth-class uses recovery copy`, `GROK_BUILD 401 maps to session login coaching`, `connection refused is unreachable not LocalLLM` |

**Gap filled:**

1. `GrokBuildAuth.requestHeaders` — was only constants-tested, not map-asserted. Now:
   - `requestHeaders includes Authorization TokenAuth version surface and User-Agent` (null model → no override)
   - `requestHeaders with non-blank model includes model override`
   - `requestHeaders trims model and omits override when blank`
2. No other pure gaps found for terminal/recovery residual candidates (already asserted).

**Blockers:**

- none

**Tests run:**

```
./gradlew.bat test --tests "com.waryway.gab.client.*" --tests "com.waryway.gab.chat.*" --tests "com.waryway.gab.ui.*" --tests "com.waryway.gab.model.*"

BUILD SUCCESSFUL in 14s
TOTAL tests=271 failures+errors=0
(baseline was 268; +3 new requestHeaders cases in GrokBuildAuthTest: 7→10)
```

**SIGNAL:** WORK_DONE
