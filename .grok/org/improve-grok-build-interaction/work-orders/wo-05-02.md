# Work Order — wo-05-02

**Order ID:** wo-05-02  
**Section:** section-05-regression-ship  
**Iteration:** `.grok/org/improve-grok-build-interaction/iterations/section-05-regression-ship/iteration-01.md`  
**Status:** done  
**Worker role:** implementer

## Task

Ship packaging readiness for the Grok Build interaction work landed in sections **01–04**. Product **main sources changed** across those sections → version bump is **required** (not the docs-only optional path).

### 1. Version bump

- Set `pluginVersion` in `gradle.properties` from **0.1.29 → 0.1.30**.
- Optional helper: `scripts/bump-version.ps1` (patches `pluginVersion` only) — or edit `gradle.properties` by hand.
- Do not change other Gradle/IntelliJ platform coordinates.

### 2. CHANGELOG

Update `CHANGELOG.md`:

- Cut or extend **`## [Unreleased]`** / add **`## [0.1.30]`** (project style: if Unreleased is the working bucket, add a clear **0.1.30** heading or bullet group so Install-from-Disk of 0.1.30 is distinguishable).
- Summarize Grok Build interaction polish from 01–04 in short bullets (operator-facing, not internal WO ids), e.g.:
  - Grok Build session recovery (missing/expired coaching, refresh without IDE restart)
  - Actionable GROK_BUILD chat/send failures (auth/network vs bare HTTP text)
  - Local LLM `/api/agent` terminal fidelity (cancel/timeout/empty finalAnswer)
  - Tool-window Grok Build failure UX + session status chip
  - Stream merge contracts preserved (regression guards)
- Keep prior Unreleased items that still apply; do not delete historical 0.1.27/0.1.28 notes.
- No fake release date requirements unless the file already uses dates.

### 3. Load / README version stamps

Stale **0.1.27** strings remain after 0.1.29 was already on the tree — update to **0.1.30** (or “see `gradle.properties` (currently **0.1.30**)”) so operators do not install an old mental model:

| File | Known stale spots |
|------|-------------------|
| `README.md` | Version line still says **0.1.27** |
| `HOW_TO_LOAD_IN_IDE.md` | Header “Current plugin version: `0.1.27`”; example zip/jar paths `…-0.1.27.zip` / `.jar`; “Select the **0.1.27** (or newer)” |

Rules:

- Keep warnings about known-bad **0.1.23 (2026-06-29)** zip (still valid).
- Keep “≥ 0.1.24” floor for agent classes if still accurate.
- Point example artifact names at **0.1.30** (or “version from `pluginVersion`”).
- Light touch only — version/load notes, not a docs rewrite.
- Optional one-line note that 0.1.30 includes Grok Build session recovery / failure UX if it helps Install-from-Disk distinguish from stream-only 0.1.27 builds — keep brief.

### 4. Out of scope

- Marketplace publish, CI redesign, rebuilding historical zips
- Running full test suite is Verifier’s job (you may smoke-compile if cheap; not required)
- Product code / test suites (wo-05-01)

## Scope

**Edit only:**

- `gradle.properties` (`pluginVersion` only)
- `CHANGELOG.md`
- `HOW_TO_LOAD_IN_IDE.md` (version stamps / load hints)
- `README.md` (version line / load hint only if still stale)
- `scripts/bump-version.ps1` — run only; do not rewrite unless broken

**Do not touch:**

- `src/main/**`, `src/test/**` (wo-05-01 owns tests)
- Plugin XML, dependencies, build scripts beyond version property
- Unrelated markdown under `.grok/org/`

## Commands (optional smoke)

```bat
.\scripts\bump-version.ps1
```

Or hand-edit `gradle.properties`. Verify:

```bat
findstr pluginVersion gradle.properties
```

Expect: `pluginVersion = 0.1.30`

## Acceptance

- [x] `pluginVersion = 0.1.30` in `gradle.properties`
- [x] `CHANGELOG.md` notes 0.1.30 / Unreleased Grok Build interaction ship content
- [x] `README.md` and `HOW_TO_LOAD_IN_IDE.md` no longer claim current version is stuck on **0.1.27**
- [x] Known-bad 0.1.23 warning retained
- [x] No source/test/product refactors; no dependency upgrades

## Worker report

**Status:** done  

**Files changed:**

- `gradle.properties` — `pluginVersion` 0.1.29 → 0.1.30
- `CHANGELOG.md` — added `## [0.1.30]` Grok Build interaction ship notes; kept Unreleased + historical 0.1.27/0.1.28 bullets
- `README.md` — version stamp 0.1.30 + brief session recovery / failure UX note
- `HOW_TO_LOAD_IN_IDE.md` — current version, zip/jar examples, select-zip hint → 0.1.30; known-bad 0.1.23 retained

**Summary:**

- Shipped packaging readiness for sections 01–04 product work: version is **0.1.30**.
- CHANGELOG distinguishes 0.1.30 Grok Build interaction polish (auth recovery, refresh session, chat failure UX, agent terminal/cancel fidelity, stream contract guards) from stream-only 0.1.27.
- README and load guide no longer advertise 0.1.27 as current; example Install-from-Disk artifacts point at 0.1.30.

**Blockers:**

- none

**Tests run:**

```
findstr pluginVersion gradle.properties  → pluginVersion = 0.1.30
(no product tests required for docs/version ship)
```

**SIGNAL:** WORK_DONE
