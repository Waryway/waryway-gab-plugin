# Work Order — wo-s02-01-02

**Order ID:** wo-s02-01-02  
**Section:** section-02-sse-tool-fidelity  
**Iteration:** `.grok/org/grok-support-agent-stability/iterations/section-02-sse-tool-fidelity/iteration-01.md`  
**Status:** done  
**Worker role:** implementer

## Task

Add focused unit tests for SSE / tool-call / finish-reason fidelity so premature silent agent exits cannot regress without a failing test.

Coordinate with **wo-s02-01-01** result contract (if not yet merged, write tests against this contract and implement thin test helpers only if production API is already present):

```text
ChatCompletionResult(
  content, toolCalls, finishReason, usage,
  cancelled = false,
  streamError = null,
  incompleteToolCallCount = 0
)
```

### Required coverage in `GabSseAccumulatorTest` (and related if needed)

1. **Empty stream** — no data lines / only `[DONE]` → content null/empty, toolCalls empty, `finishReason == null`, incomplete count 0.
2. **Error event** — `data: {"error":{"message":"…","type":"server_error"}}` still surfaces `SseEvent.Error` (existing). If accumulator tracks `streamError`, assert it is set after processing that line. Document whether `parseSseLines` exposes error (may only apply when using `processSseLine` + events / `toResult`).
3. **finish_reason variants** — separate cases or parameterized: `"tool_calls"`, `"stop"`, `"length"` captured; chunk with `"finish_reason":null` (or missing finish) leaves Kotlin `null` (not string `"null"`).
4. **Incomplete tool_calls** — stream ends with tool builder missing `id` and/or `name` (e.g. only arguments fragments, or name without id if policy flags incomplete) → assert `incompleteToolCallCount > 0` and/or recovered tool per implementer policy; must **not** look like a clean zero-tool stop with no signal.
5. **Fragmented happy path** — keep/extend existing multi-chunk tool test (id+name first, args later, finish `tool_calls`).
6. **Optional client-level** — if testable without HTTP: pure functions / package-visible helpers for cancel flag mapping. Do **not** spin real network. Prefer unit tests over integration.

### Style

- Match existing Kotlin test style (`kotlin.test`, class `GabSseAccumulatorTest`).
- Prefer small focused tests with clear names.
- Do not delete existing tests; update only if API fields require it.

## Scope

**Edit only:**

- `src/test/kotlin/com/waryway/gab/client/GabSseAccumulatorTest.kt`
- `src/test/kotlin/com/waryway/gab/client/GabClientJsonTest.kt` (only if finish/tool parse helpers need coverage)
- New test file under `src/test/kotlin/com/waryway/gab/client/` only if splitting is clearer — prefer extending existing

**Do not touch:**

- Production source except if wo-s02-01-01 already left an unfinished public test hook — prefer coordinating via result fields, not new production APIs beyond the contract
- `AgentSession` / UI / settings

If production fields from wo-s02-01-01 are missing when you run: implement **tests first** against the contract above, then either wait/retry once after noting blocker, or add the minimal field wiring only if absolutely required for compile (prefer marking blocked with precise missing symbols over expanding scope).

## Commands (run from repo root)

```powershell
.\gradlew.bat test --tests com.waryway.gab.client.GabSseAccumulatorTest --tests com.waryway.gab.client.GabClientJsonTest
```

Fallback: `.\gradlew.bat test`

## Acceptance

- [x] Empty stream covered
- [x] Error event covered (event and/or streamError)
- [x] finish_reason variants covered (`tool_calls`, `stop`, `length`, null)
- [x] Incomplete tool_calls covered with non-silent signal assertion
- [x] Fragmented happy path still green
- [x] Scoped tests pass when paired with wo-s02-01-01 implementation

## Worker report

**Status:** done  
**Files changed:**

- `src/test/kotlin/com/waryway/gab/client/GabSseAccumulatorTest.kt`

**Summary:**

Extended `GabSseAccumulatorTest` against the landed `ChatCompletionResult` contract (`cancelled`, `streamError`, `incompleteToolCallCount`). Added empty-stream cases (no lines / only `[DONE]` / blank non-data), error event + `parseSseLines` `streamError` recording, finish_reason variants (`tool_calls`, `stop`, `length`, JSON null, missing), incomplete builders (missing id, missing name, args-only, mixed complete+incomplete), and cancel mapping that does not invent `finishReason=stop`. Kept fragmented happy path. Coordinated with parallel wo-04-01-02 complementary cases in the same class (no conflicting rewrite). Scoped run: GabSseAccumulatorTest 23 + GabClientJsonTest 9 = 32 green.

**Blockers:**

- None
