# Plugin metadata
pluginGroup = com.waryway.gab
pluginName = Waryway Agent
pluginId = com.waryway.gab
pluginVersion = 0.1.46

# See https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html
# 243 = 2024.3
pluginSinceBuild = 243

# Gradle must run on JDK 21 — Kotlin 1.9.x cannot parse Java 25 (GoLand JBR).
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot

# Opt-out flags
kotlin.stdlib.default.dependency = false
# Disabled until Kotlin plugin supports Gradle 9 configuration-cache listeners.
org.gradle.configuration-cache = false
org.gradle.caching = true

# Download sources for better IDE navigation
org.jetbrains.intellij.platform.downloadSources = true

# Use cache redirector if desired (can be disabled for direct)
# org.jetbrains.intellij.platform.useCacheRedirector = true

